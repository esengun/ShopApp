@*@page
@model ShopApp.WebUI.Views.Shared._categoriesModel
@{
}*@

@model List<Category>

<div class="list-group">
	@foreach (var category in Model)
	{
		<a href="#" class="list-group-item list-group-item-action">@category.Name</a>
	}
</div>
